# テストケース & 証跡 (TESTCASES.md)

## 1. テスト手順と期待結果

| テストID | 機能カテゴリ | テスト手順 | 期待される結果 | 結果 |
|---|---|---|---|---|
| **TC-01** | APIキー設定 | 右上の「APIキー未設定」ボタンを押下し、有効なGemini APIキーを入力して「保存する」を押下 | `localStorage` にキーが保存され、ヘッダーの表示が緑色の「APIキー設定済み」に変化すること。 | PASS |
| **TC-02** | APIキー削除 | モーダルで入力欄を空（未入力）にした状態で「保存する」を押下 | `localStorage` からキーが削除され、ヘッダーが赤色の「APIキー未設定」に戻ること。 | PASS |
| **TC-03** | 音声プレビュー | 「ファイルを選択」から音声ファイル（.mp3, .m4a等）を選択 | プレビューエリアが表示され、`<audio>` プレイヤーで音声が再生・確認できること。 | PASS |
| **TC-04** | バリデーション | APIキー未設定の状態で「✨ 文字起こし＆要約を実行」ボタンを押下 | 設定要求アラートが表示され、APIキー設定モーダルが自動的に開くこと。 | PASS |
| **TC-05** | バリデーション | 音声ファイル未選択の状態で「✨ 文字起こし＆要約を実行」ボタンを押下 | 「音声ファイルを選択してください。」というアラートが表示されること。 | PASS |
| **TC-06** | 解析・パース | ファイル選択・キー設定の状態で「✨ 文字起こし＆要約を実行」ボタンを押下 | ボタンが非活性化（「⏳ 音声を解析中...」）となり、処理完了後に要約・ポイント・全文テキストが各タブに正しく分割表示されること。 | PASS |
| **TC-07** | タブ切り替え | 「📊 要約 & 要点」と「📝 全文文字起こし」のタブを交互にクリック | 表示コンテンツがスムーズに切り替わり、アクティブなタブのスタイルが変化すること。 | PASS |
| **TC-08** | コピー機能 | 「要約をコピー」または「全文をコピー」ボタンを押下 | 各テキストがクリップボードにコピーされ、ボタン文言が「コピーしました！」に2秒間変化すること。 | PASS |
| **TC-09** | エラーハンドリング | 無効なAPIキーを設定した状態で実行ボタンを押下 | 赤枠のエラーアラートが表示され、APIから返却されたエラーメッセージが提示されること。 | PASS |

## 2. エビデンス・確認結果
- **検証環境**: Google Chrome / Apple Safari（PCおよびスマートフォン環境）
- **確認事項**: 
  - 音声ファイルの Base64 化処理（`FileReader`）が正常に完了すること[cite: 8]。
  - APIレスポンス文字列の `split()` 処理により、セパレーター形式に沿って各要素が正常に UI へ分配・描画されること[cite: 8, 9]。
