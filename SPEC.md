# 講義ノートAI 仕様書 (SPEC.md)

## 1. アプリ概要
- **目的**: 講義や自習の音声データから文字起こし・要約・要点を即座に自動生成し、学習効率を高める。
- **ターゲット**: スマホ/PCブラウザから利用する学生・社会人学習者。
- **構成**: HTML/CSS/JS による 1 ページ構成（Single Page App）。

## 2. ユーザー導線（UX Flow）
1. 起動時に `localStorage` から Gemini APIキー を取得。未設定の場合はモーダルを提示。
2. 音声ファイル（MP3/M4A/WAV等）を選択。プレイヤーで再生確認。
3. 実行ボタン押下で Base64 エンコード処理 ➔ Gemini API (`gemini-2.5-flash`) へリクエスト送信。
4. ローディング表示（ボタン非活性＋メッセージ提示）。
5. レスポンス受領後、指定セパレーターでレスポンスをパースし「3行要約」「要点」「全文文字起こし」をタブ別描画。
6. コピーボタンでクリップボードへ書き出し。

## 3. エラーハンドリング要件
- APIキー未設定時: 設定モーダルの自動表示。
- 音声ファイル未選択時: 警告ダイアログの表示。
- API通信失敗（400/401等）: エラーメッセージ枠を赤色表示し、原因（キー不正・形式エラー等）を提示。